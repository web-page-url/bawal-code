/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  
};
module.exports = {
  output: 'static', // Set output to 'static' for static export
};


export default nextConfig;
